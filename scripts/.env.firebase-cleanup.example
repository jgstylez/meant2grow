# Copy to .env.firebase-cleanup and fill in values.
# Used by run-firebase-cleanup.sh to send failure emails via MailerSend.
# FROM_EMAIL must be a verified domain in MailerSend.

MAILERSEND_API_TOKEN=your_mailersend_api_token
FROM_EMAIL=noreply@meant2grow.com
